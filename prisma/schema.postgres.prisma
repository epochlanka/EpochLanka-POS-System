generator client {
provider = "prisma-client-js"
}

datasource db {
provider = "postgresql"
url      = env("DATABASE_URL_CLOUD")
}

// NOTE: Models here will mirror schema.sqlite.prisma exactly.
// We keep them separate to allow Prisma to handle DB-specific migrations smoothly.
